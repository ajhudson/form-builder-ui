describe('form validator tests', () => {
  it.skip('should determine if a mandatory form field which is empty is not valid', () => {
    /*
    const formConfig = {
        "id": 1,
        "formName": "test form",
        "fields": {
            "fieldId": 1,

        }
    }

    expect(true).toBeFalsy();
    */
  });
});
